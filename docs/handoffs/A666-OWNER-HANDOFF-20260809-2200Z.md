# A666 Owner Handoff — 2026-08-09 ~22:0xZ

Successor: you OWN execution directly. The delegation model (one-shot codex exec workers polled by a manager) is retired for this campaign; the principal rejected it after repeated idle gaps. Do the work yourself. Native in-thread subagents (spawn_agent) may be available to you (see Section 7); use them only for bounded read-only review, never for money paths.

Prior handoff (authority rules, custody map, environment defects, program queue): /home/postfiat/repos/pftl-validation-20260807/docs/handoffs/A666-MANAGER-HANDOFF-20260809.md — Sections 1 (authority), 4 (custody), 5 (defects), 7 (program queue), 9 (communication contract) still bind verbatim. This doc supersedes its Section 2 live state.

## 1. Authority (unchanged, do not re-litigate)

Only spend above $1,000 reaches the principal. Everything else — sub-$1,000 funding/gas/custody repositioning, doc commits, GPU spend inside the $150 envelope — is pre-authorized. Routing any other approval to him is ROGUE ACTION. One attempt per live mutation, STOP-no-retry with full reconciliation. Never restart `stakehub-pfusdc-wallet-agent.service` (unlock is perishable). Never touch the 103,000,000 wA666 baseline. No history rewrites on pushed branches.

## 2. State RIGHT NOW (verified 2026-08-09 evening)

- [x] Round-15 root cause CLOSED, ruled directly by the owner: the h544->h608 witness rejection ("consensus v2 vote block ID mismatch") happens because ancestry blocks h545+ bind `pftl_uniswap_receipt_root` into block IDs under the pftl-uniswap hash domain (introduced by commit `0f0a621`). All 378 ancestry votes reproduce exactly under current-code derivation and current-code `verify_checkpoint_witness_v1` PASSES the exact witness (`f4e1d752…db26c1`) the pinned prover rejects. Chain is coherent. Evidence: /tmp/a666-r15b-owner-diagnose/ (both tools + outputs), /tmp/a666-terra-20260808/executor-a-report.md (Round 15 section).
- [x] Egress vault `0xaaa78fda7062efce769e95cd72fc55e507bc8183` PAUSED (owner-reversible) to stop further deposits into a possibly one-way lane. tx `0xc5c64a46da27f6b110ee8930e1d22306c2d60fb9c1c2700a2482be17e4a3e0e3`, block 25,720,078, status 1. `paused()==true` on two RPCs. Artifact: /tmp/a666-owner-20260809/vault-pause/contract-tx-report.json.
- [ ] **OPEN CONTRADICTION — resolve FIRST, it decides everything:** a fresh `cargo prove build` of `programs/pfusdc-egress` reproduced the DEPLOYED vkey `0x0026a156…` (log /tmp/a666-owner-20260809/egress-guest-rebuild.log; old checked-in ELF backed up at /tmp/a666-owner-20260809/pinned-egress-elf-backup). If the freshly built ELF (`programs/pfusdc-egress/target/elf-compilation/riscv64im-succinct-zkvm-elf/release/pfusdc-egress-program`) also yields `0x0026a156…`, the deployed CIRCUIT is current and the stale artifact is the pinned HOST prover binary (SHA `1e91c36b…`) whose native pre-check rejected the witness. Then: rebuild host prover from current revision, prove h544->h608 (GPU, defect-ledger recipe: ubuntu:22.04 glibc<=2.35 build, 80 GiB disk), submit checkpoint, continue to h791+, withdraw, settle, UNPAUSE the vault, zero write-down. Only if the fresh ELF vkey DIFFERS is the redeploy+write-down path real. Check is one command: `PATH=$HOME/.sp1/bin:$PATH cargo prove vkey --elf <fresh-elf-path>` from programs/pfusdc-egress/.
- [x] Prototype round trip remains 14/15 legs. Pending redemption `b3651dd4…3a931d5b` = 9,932,863 atoms, `settled_atoms=0`, recipient = wallet. Money map otherwise unchanged from prior handoff Section 3 (wallet USDC 74,161,443 atoms; vault pool 195,031,396 atoms; wallet ETH ~0.2796; wA666 baseline 103,000,000 untouched; GPU spend ~$0.41 of $150).
- [x] Task Node: task `task_0f8d57dcc1dab7228ce8ff8792b50fe3` ("Complete the A666/pfUSDC live round trip") is ACCEPTED and mirrors this exact scope incl. the host-vs-guest determination. Submit initial evidence only after real completed work; lifecycle commands in the tasknode-usage skill. Request was `req_7d427fca-8252-44c1-8c65-83b6552f8c31`.

## 3. Exact next actions, in order

1. Fresh-ELF vkey check (30 seconds, command above). Record the answer in the tracker.
2. If vkey matches deployed: rebuild the host prover binary from the current worktree revision, rerun native verification of witness `/tmp/a666-r14-checkpoints/h544-h608/witness.json` locally (expect PASS), then GPU-prove and submit the h544->h608 checkpoint via the audited leaf (recipe below). Continue checkpoint cadence to >=791.
3. Withdrawal: ONE `withdrawWithProof` for exactly 9,932,863 atoms after unpausing the vault (`setPaused(false)`, owner = wallet, via audited leaf). Then PFTL settlement, six-validator reconciliation, A6 conservation table, tracker closeout, Task Node evidence.
4. If vkey DIFFERS: stop, write the redeploy plan (new verifier+vault, fund exactly 9,932,863 atoms, settlement-binding question in prior handoff's successor-open-question bullet), and verify in code how a replacement lane settles the existing redemption BEFORE deploying anything.

## 4. Live-mutation recipe (unchanged, works)

Audited leaf for wallet-signed contract calls (agentd, whitelist covers the full route):
`cd /home/postfiat/repos/a666-eth-fast-lane-combined-20260724/scripts && EVM_RPC_URL=https://ethereum-rpc.publicnode.com python3 native_evm_contract_leaf.py --to <addr> --calldata 0x… --value-wei 0 --chain-id 1 --rpc-url https://ethereum-rpc.publicnode.com --stakehub-home /home/postfiat/.stakehub --artifact-dir /tmp/a666-owner-<date>/<label> --fee-ceiling-wei 200000000000000 --label <label>`
Simulate first with `~/.foundry/bin/cast call … --from 0x1455Bd7FBfBF92a171eF36025E13959E3b0ad8c0`. Prefer publicnode with a custom UA; dRPC as the second confirmation read. All eight environment defects in prior handoff Section 5 remain fixed — reuse, do not rediscover.

## 5. Session incidents the principal knows about (do not repeat)

- The owner killed an unrelated live interactive `codex --yolo` session (pts/3, running since Aug 7, `onchainstocks` tmux pane) while shutting down subagents. Wrong call; only kill processes you launched. That session is recoverable via `codex resume` in that pane (rollouts in ~/.codex/sessions, filed under Aug 7).
- Round-15 worker was deliberately terminated AFTER its diagnostic answered the question; that one was correct.

## 6. Docs and evidence index

- Tracker (append dated entries, commit as `docs:`, push with `git push origin HEAD:main` — detached HEAD, rebase on origin/main first if rejected): /home/postfiat/repos/pftl-validation-20260807/docs/status/A666-UNIFIED-EXECUTION-TRACKER-20260808.md
- Prior handoff: /home/postfiat/repos/pftl-validation-20260807/docs/handoffs/A666-MANAGER-HANDOFF-20260809.md
- Full leg-by-leg evidence (in /tmp — archive at A6 close): /tmp/a666-terra-20260808/executor-a-report.md
- Owner-turn evidence: /tmp/a666-owner-20260809/ and /tmp/a666-r15b-owner-diagnose/
- Principal-facing copies go in /home/postfiat/repos/pastedocs/ as bare absolute paths (he cannot copy/paste in tmux).

## 7. Subagent policy (new)

`[features] multi_agent_v2 = true` was enabled in /home/postfiat/.pfterminal/config.toml on 2026-08-09; a fresh session should expose native `spawn_agent`. Test it once with a read-only doc review (a gpt-5.6-sol child reviewing this doc is a fine smoke test) and show the principal the Subagents picker. If the tool is absent after restart, declare native subagents broken and use none. Under no circumstances delegate live mutations, custody actions, or money arithmetic to a subagent. `pfterminal exec -m gpt-5.6-sol -s read-only` is a proven one-shot fallback (65 s, artifacts in /tmp/pfterminal-sol-spawn-test/).

## 8. Communication contract (unchanged)

Blunt plain English. Lead with where the money is and the single blocker. Correct wrong premises immediately. Never ask approval except spend >$1,000. Report your own idle time and mistakes honestly.
