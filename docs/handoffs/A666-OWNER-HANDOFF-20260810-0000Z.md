# A666 Owner Handoff — 2026-08-10 ~00:0xZ (MID-FLIGHT LIVE MUTATION — READ SECTION 2 FIRST)

Successor: you OWN execution directly. No subagents (principal order). Predecessor was stopped
mid-flight by the principal while completing a live PFTL transaction; finish Section 2 before
anything else. The principal may have upgraded the environment with:
`curl -fsSL https://github.com/agtico/PfTerminal/releases/download/rust-v0.1.27/install.sh | bash`
— a fresh PFTerminal; re-verify tooling assumptions, do not assume prior session config.

Authority, custody, defects, communication contract: prior handoffs bind verbatim —
- /home/postfiat/repos/pftl-validation-20260807/docs/handoffs/A666-MANAGER-HANDOFF-20260809.md (Sections 1, 4, 5, 9)
- /home/postfiat/repos/pftl-validation-20260807/docs/handoffs/A666-OWNER-HANDOFF-20260809-2200Z.md
Short form: only spend >$1,000 reaches the principal; everything else pre-authorized; one attempt
per live mutation, STOP-no-retry; never restart `stakehub-pfusdc-wallet-agent.service`; never touch
the 103,000,000 wA666 baseline; no history rewrites.

## 1. Session verdicts already CLOSED (do not redo)

- [x] Fresh-ELF vkey check: fresh egress guest vkey is
  `0x0015b046ba4b80c0ca7e2d9429a1f5fd88bc6d1d328cca6acec29ffdf48a9d87` (ELF sha256
  `4d5f8449…761c67e0`), DIFFERS from deployed `0x0026a156…`. The 20260809-2200Z handoff's
  "fresh rebuild reproduced deployed vkey" was a mismeasurement (that session hashed the
  old checked-in ELF). Old lane permanently dead. Evidence:
  /tmp/a666-owner-20260809/vkey-recheck/RESULT.txt. Tracker commit `26a7e7a`.
- [x] Settlement-binding code verification: pending redemption `b3651dd4…3a931d5b`
  (9,932,863 atoms) can NEVER settle via a replacement vault — observation match at
  nav_vault_asset_execution.rs:1442-1455 requires the packet's old vault address; no
  cancel/rebind op exists; old vault has no verifier setter; dishonest attestation and
  unbacked re-mint refused. Plan of record (committed `5af423c`):
  /home/postfiat/repos/pftl-validation-20260807/docs/plans/A666-EGRESS-LANE-REDEPLOY-PLAN-20260809.md

## 2. MID-FLIGHT: bucket write-off transaction — finish this FIRST

State at handoff: the impair transaction is SIGNED and SITTING IN validator-2's MEMPOOL,
NOT yet included in a block. One finality attempt was made at validator-2 and was rejected
with `rpc_finality_wrong_proposer`: deterministic proposer for height 792 view 0 is
`validator-0`; the error explicitly instructs to retry the SAME signed request at validator-0.
This is the designed delivery mechanism, not a second mutation attempt — same tx_id, do NOT
re-sign, do NOT rebuild, do NOT create a new operation.

Facts:
- tx_id `c2fefc40598fc9169fe2b1dead1e0283de55e89ad65eea85d88f854257582170ee23cadf230c145a292d3adb42ee84f8`
- kind `vault_bridge_bucket_impair`, source/operator (issuer) `pf23d8831301aa1cce6fdd7bf4a2db2aead1619ba8`,
  fee 23, sequence 228
- operation: bucket `5d5abc04…3665c536` (old-vault lane), updated_counted_value_atoms 0,
  impairment_factor_bps 0 (= floor(0*10000/195,031,396), exact), policy_hash `5025bdfe…4137158`,
  reason_hash `e2001344630bfe3664caa0cffa3b1e2229d5a6a34f3640e8180950939c7beba1e465cebebe9572bcab870f601204a726`
  (sha3-384 of reason doc)
- artifacts (on validator-2, 66.42.48.39): /var/lib/postfiat/validator-2/a666-impair-20260809/
  {reason.md, impair.operation.json, impair.quote.json, impair.signed.json, impair.submit.json,
  impair.finality.response.json}
- fleet before submit: 6/6 converged h791 tip `e453a0af…` mempool 0; after submit validator-2
  mempool 1 (this tx)

EXACT NEXT ACTION — submit the SAME signed tx via the finality RPC on validator-0
(64.176.220.75, RPC 127.0.0.1:27650, raw TCP newline-terminated JSON, NOT HTTP):
1. scp the signed file: `scp root@66.42.48.39:/var/lib/postfiat/validator-2/a666-impair-20260809/impair.signed.json /tmp/` then to validator-0, or read it over ssh.
2. On validator-0, take fresh `postfiat-node status --data-dir /var/lib/postfiat/validator-0`
   for block_height/block_tip_hash/state_root.
3. Send over TCP to 127.0.0.1:27650 one line:
   `{"version":"postfiat-local-rpc-v1","id":"impair-finality","method":"mempool_submit_signed_asset_transaction_finality","params":{"signed_asset_transaction_json":"<compact signed json as string>","proxy_required_current_height":<height>,"proxy_required_parent_hash":"<tip>","proxy_required_state_root":"<state_root>","proxy_readiness_timeout_ms":90000}}`
   (socket pattern: scripts/a666-ce22-finality-op.py `rpc_call`, worktree
   /home/postfiat/repos/a666-eth-fast-lane-combined-20260724; node binary on hosts:
   /opt/postfiat/releases/pnok-private-fix-2246d25-orchard1/postfiat-node)
4. If proposer rotated again, the error names the correct validator; follow it. Duplicate
   submission of the same signed tx is safe (same tx_id, sequence-guarded).
5. Verify inclusion: all six validators (hosts in
   /home/postfiat/repos/a666-eth-fast-lane-combined-20260724/docs/evidence/a666-joe-mainnet-e2e-20260728/proposer-hosts.json)
   report height >=792, same tip, mempool 0, and `vault-bridge-status` for asset
   `02c46a36…005d7b` shows the `aaa78fda` bucket with counted_value_atoms 0,
   impairment_factor_bps 0, redemption still pending (expected, tombstone).
6. Record in tracker (append dated entry, commit `docs:`, push `git push origin HEAD:main`
   from /home/postfiat/repos/pftl-validation-20260807; rebase on origin/main if rejected).
7. If the finality RPC returns a rejection OTHER than wrong-proposer/duplicate: STOP-no-retry,
   full reconciliation (6/6 status + bucket + redemption + issuer account sequence), record,
   and re-derive before any new attempt.

## 3. After Section 2: remaining plan (plan-of-record Section 3, in order)

1. Governed route registration for the NEW lane (route_epoch advance; new verifier vkey
   `0x0015b046…`; predict vault address first — profile binds vault, verifier binds profile
   hash, vault binds verifier; mirrors July deployment order).
2. Deploy new PFTLFinalityVerifierV1 (seed `initialCheckpointCommitment`/`initialFinalizedHeight`
   at CURRENT height — no historical re-proving) + new ERC20BridgeVaultL1 on Ethereum mainnet.
   Gas sub-$1,000 pre-authorized. Audited leaf recipe in 20260809-2200Z handoff Section 4.
3. Custody: the two new addresses need agentd whitelisting — ONE principal passphrase command,
   stated as mechanical fact with exact text once addresses exist. Only principal touchpoint.
4. Fresh ~$10 round trip through the new lane: depositV2 10,000,000 atoms -> mint -> burn_to_redeem
   (new bucket) -> GPU-prove fresh checkpoint+receipt under new vkey (defect-ledger recipe:
   ubuntu:22.04 glibc<=2.35, 80 GiB disk, vast.ai, ~$146 of envelope left) -> one
   `withdrawWithProof` -> `redeem_settle` with observations matching the NEW vault -> 6/6
   reconciliation.
5. A6 conservation restated with the 195,031,396-atom write-off; archive /tmp evidence
   (/tmp/a666-owner-20260809/, /tmp/a666-r15b-owner-diagnose/, /tmp/a666-terra-20260808/executor-a-report.md);
   tracker closeout; Task Node evidence for `task_0f8d57dcc1dab7228ce8ff8792b50fe3`.
6. Prevention (fix round): checkpoint-cadence automation with alarms; day-zero contract/chain
   compatibility survey; block-ID-change => bridge handover rule.

## 4. Money map (unchanged this session — impair is accounting only, no funds moved)

| Where | Amount |
|---|---|
| Wallet USDC 0x1455Bd7F… | 74,161,443 atoms |
| Old vault 0xaaa78fda… (PAUSED, stranded, being written off) | 195,031,396 atoms incl. 9,932,863 pending claim |
| Wallet ETH | ~0.2796 |
| wA666 baseline | 103,000,000 — UNTOUCHABLE |
| GPU spend | ~$3.8 of $150 |

## 5. Access notes

- Validators: root SSH by IP (BatchMode works), hosts file above. validator-2 holds the issuer
  key at /var/lib/postfiat/validator-2/a666-joe-e2e-20260728/pfusdc-issuer-key.json (never print
  contents). RPC ports: v0 27650, v2 27652 (others: check `pgrep -af rpc-serve` per host);
  raw TCP JSON, one line in, response, close.
- Ethereum reads: publicnode with custom UA first, dRPC second. All eight environment defects in
  the manager handoff Section 5 remain fixed — reuse.
- Principal-facing copies: /home/postfiat/repos/pastedocs/ as bare absolute paths.
