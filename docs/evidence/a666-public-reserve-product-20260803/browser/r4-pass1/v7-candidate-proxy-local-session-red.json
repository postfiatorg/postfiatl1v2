{
  "schema": "postfiat.a666.r4.v7-candidate-proxy-local-session-red.v1",
  "recorded_utc": "2026-08-05T12:05:00Z",
  "status": "RED",
  "vector": "candidate proxy stage requires a local-session principal before wallet mutation methods are enabled",
  "ruling": "trustline finality RED root cause is staging configuration, not product/runtime; launcher guard must exist before any restage",
  "command": "scripts/test-a666-r4-candidate-proxy-local-session",
  "exit_code": 2,
  "runtime_ms": 8,
  "matched_vectors": 1,
  "failure": "missing local-session principal guard",
  "launcher_under_test": "scripts/a666-r4-candidate-proxy-stage",
  "required_contract": [
    "principal sourced from a mode-600 ready-file PATH (WALLET_PROXY_LOCAL_SESSION_PRINCIPAL file-sourced, never an inline literal)",
    "launcher refuses missing/empty principal before any service change (guard precedes first proxy stop in source order)",
    "readiness proves GET /api/bridge/local-session succeeds (ok == true, token length >= 32) without displaying the token"
  ],
  "current_launcher_state": "zero references to WALLET_PROXY_LOCAL_SESSION_PRINCIPAL or /api/bridge/local-session; the staged proxy (pid 3318900) runs with no local-session principal, so wallet mutation methods are refused with proxy_auth_required",
  "boundaries": {
    "live_proxy_stage_called": false,
    "service_restarted": false,
    "ledger_traffic_submitted": false,
    "ready_file_address_or_token_exposed": false,
    "static_isolated_fixture_only": true
  },
  "syntax_check": "bash -n exit 0",
  "credential_scan": "no address, token, principal-file contents, or secret values in test or evidence",
  "stakehub": "untouched; active 5/5",
  "next_step": "ruling on adding the principal guard to the launcher (harness/staging change) and restaging the candidate proxy"
}
