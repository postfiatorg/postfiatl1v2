A666 recovery CI rejection-policy grep evidence
Date: 2026-08-04
Current-tree checker status: GREEN. The checker is wired as a merge-blocking CI
step immediately after the regression-manifest step. No product/test code was
changed, and no finding was baselined or allowlisted.

Rule contract (condition semantics fix, this change):
- Success-only report/evidence write: a report/evidence write under an
  if condition whose guard is an affirmative success test: `if ok`,
  `if result["ok"]`, `if accepted`, `status == "ok"`. These still FAIL.
- Failure-path emission: a report/evidence write under an explicit failure
  guard is CLEAN: inequality against a success operand (`checks["status"] != "ok"`,
  `ok != ...`), negation of a success expression (`if not build["ok"]`,
  `if !result.ok`), equality with false (`ok == false`, `ok is False`),
  and `is not True`. Checker implementation: is_failure_condition() in
  scripts/check-a666-recovery-ci-policy, classifying the comment-stripped
  condition with FAILURE_COMPARISON_RE and FAILURE_NEGATION_RE before the
  64-line bounded branch scan. No path allowlists or standing known-findings
  were added.
- Direct proving in test paths, literal result status in emitted schema
  objects, and the retired-identifier runtime rule are unchanged from the
  prior grammar revision.

Condition-semantics repair (2026-08-04):
- Prior false positives, both failure-path emission sites, now clean:
  scripts/testnet-monitor-snapshot:924-925
    if args.alert_spool_dir is not None and checks["status"] != "ok":
        report["alert_event"] = rel(write_alert_event(report, args.alert_spool_dir))
  scripts/testnet-validator-doctor:981-992
    if not build["ok"]:
        ... args.output.write_text(json.dumps(report, ...))
- Root cause: NEGATED_SUCCESS_RE only recognized `not ok` / `!ok` with the
  keyword immediately adjacent, so `!=` comparisons and `not x["ok"]`
  subscript negations were misclassified as success guards.

Commands and results:
1. scripts/test-check-a666-recovery-ci-policy
   exit: 0
   final: a666_recovery_ci_policy_selftest=ok clean=1 failure_path_clean=2 rejected=6
   clean coverage: computed report status; ordinary RPC response schema status;
   test/e2e emitted schema fixture; operational absence-check identifier.
   clean failure-path vectors (exact current sites):
   - `if args.alert_spool_dir is not None and checks["status"] != "ok": write_alert_event(...)`
   - `if not build["ok"]: args.output.write_text(...)`
   rejection coverage: direct proving; success-only write under `if accepted:`;
   success-only write under `if result["ok"]:`; success-only write under
   `if status == "ok":`; runtime retired identifier; literal result status in
   emitted report.
2. scripts/check-a666-recovery-ci-policy
   exit: 0
   finding_count: 0
   rule_counts: direct-proving-in-test=0 success-only-report-evidence-write=0
                retired-identifier-runtime=0 literal-result-boolean-schema-emitter=0
   final: a666_recovery_ci_policy=ok enumerated=15098 scanned=492
3. python3 YAML parser assertion for .github/workflows/rust-ci.yml
   exit: 0
   final: workflow_yaml_parse=ok policy_step_index=3 follows_manifest_step=true selftest_before_checker=true
   adjacency: step "A666 recovery CI rejection policy" (runs
   scripts/test-check-a666-recovery-ci-policy then
   scripts/check-a666-recovery-ci-policy) immediately follows step "A666
   recovery manifest and standalone regressions" in the test job.
4. git diff --check on the four owned paths
   exit: 0

Excluded category counts (GREEN run):
excluded[docs]=954
excluded[evidence]=6784
excluded[site]=0
excluded[test]=88
excluded[tests]=49
excluded[*_tests.rs]=16
excluded[*.test.*]=25
excluded[fixtures]=15
excluded[generated]=0
excluded[vendor]=0
excluded[target]=0
excluded[node_modules]=0
excluded[dist]=0

Remaining genuine file:line findings: none.
