DETERMINISM RULING — Troll Burzum, 2026-08-07 (root-caused personally, semantic jq -S diffs)

VERDICT: Resolver determinism PASS. resolve.py is deterministic; re-render from immutable
sources + values-S1.json reproduces 14/16 committed packets byte-identical after output-root
path normalization, and re-renders are reproducible. No nondeterminism defect.

RESIDUAL 1 — native-leg3a-export-debit: PASS-documented. The committed packet omits
operation.ethereum_packet_digest / ethereum_packet_schema_version and DECLARES the amendment
in resolved_fields as "OMITTED-OPTIONAL-PER-FIRE-10-RULING" (both json pointers listed).
Fully auditable source→resolved delta. No gap.

RESIDUAL 2 — native-leg1-bridge-in: PROVENANCE GAP (documentation defect, NOT executable
integrity). Committed packet vs fresh render:
  - /command_template and /executor amendments: DECLARED in resolved_fields (FIRE-20L
    finalize-only/claim-only split; same-batch admission invalid because the claim ceiling
    requires finalized route backing). Auditable.
  - /abort_conditions 5→9 entries, /custody_signing_leaf kind/rule text,
    /evidence_output_paths 3→22 entries: amended post-resolution, NOT declared in
    resolved_fields, NOT present in the immutable HELD source (verified: source has 5
    abort_conditions, old custody kind, 3 evidence paths). These are the FIRE-20L
    execution-prep hardening edits. A re-render from sources cannot reproduce them, and
    resolved_fields understates the delta.
IMPACT: none on executable integrity — binding-S1.json pins the committed bytes 16/16
(leg1 b6ae7b84…), runbook §3 hashes match, and the execution-hardened packet is the
correct artifact (the render's older same-batch finalize-claim sequence is the INVALID
one). The gap is auditability only.
REMEDY CONSTRAINT: resolution-rules.md lineage rule makes the 16 HELD sources and
authorization-binding-native-v1.json immutable; back-propagation is prohibited without a
new stage binding. Disposition: disclose in END-TO-END-VALIDATION-REPORT.md as a declared
documentation defect; recommend next stage binding (S2) regenerate from updated sources or
attach a resolved_fields supplement for leg1.

EVIDENCE: /tmp/ghash-val/determinism.txt (byte diffs), semantic field counts verified by
Troll via jq -S (source 5 abort_conditions / old custody kind / 3 evidence paths vs
committed 9 / two-phase kind / 22), render scratch preserved at /tmp/burzum-resolved-rc-scratch
(validation worktree left clean).
