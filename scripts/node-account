#!/usr/bin/env bash
set -euo pipefail

DATA_DIR="${DATA_DIR:-.postfiat/node0}"
ADDRESS="${ADDRESS:-pfrecipient000000000000000000000000000000}"

cargo run -p postfiat-node -- account \
  --data-dir "$DATA_DIR" \
  --address "$ADDRESS"

