# Combined devnet deployment preparation — 2026-09-21

**Unsigned preparation only. No deployment, signing, backup export, restart,
service change, or Task Node action was performed.**

Release: `combined-devnet-20260921`. Qualified source tip:
`a5b1e757068864fe18e5c5d3498697942293b1d5`.
Executable SHA-256:
`051ad12ce22c33f2370388259d754a9c1016a8edc1b52db7d2852fe78f6fbc7d`.

The supplied qualification and green CI are accepted without re-running them.
[Existing build provenance](../release-repair-20260918/node-builds.json)
records build source `03e422a722eba5bc37e9b3ea71ecae81c02d6f45`; the build
script embeds its eight-character revision `03e422a7`. The signed manifest
must use that runtime revision, while this preparation records the qualified
release tip separately. This packet does not rebuild or relabel the executable.

## Contents

- [RELEASE-ID.txt](RELEASE-ID.txt): release, executable, source, and rollback identities.
- `rootfs/etc/`: 12 canonical systemd units, 12 environment files, six runtime
  bindings, topology, and both circuit metadata files.
- `stage-report.template.json` and `validator-bindings.signing.template.json`:
  generated report/bindings with the local stage prefix replaced by `@STAGE@`.
  They are templates, not inputs to pass directly to the node.
- [manifest-input.unsigned.json](manifest-input.unsigned.json): all reviewed
  hashes and signing parameters; deliberately not a deployment-manifest schema.
- [prepare-stage.py](prepare-stage.py): regenerates a portable, real rootfs stage
  using the qualified executable, checks every generated file against this packet,
  and installs the existing public trust file locally.
- [SIGNING.md](SIGNING.md): exact key-holder command, expanded node invocation,
  validity-window choice, and expected signed output.
- [PREFLIGHT.md](PREFLIGHT.md): actual offline results and signature/scope blocks.
- [DEPLOY-SHEET.md](DEPLOY-SHEET.md): future deployment and recovery commands.
- `observed/`: permitted validator-0 unit/layout observations and redacted old
  manifest identity, not a current six-host health observation.

The original local generated stage remains at
`~/.cache/deploy-prep-20260921/validator-stage`.
The executable was read from
`~/.cache/qualify-fix-20260918/binaries/candidate-1`, as recorded in the
existing qualification. Raw permitted reads and the existing public trust file
remain under `~/.cache/deploy-prep-20260921/observed`.

## Layout and redaction

Inputs were generated by `deployment-validator-units-stage`, following the
[signed-manifest runbook](../../docs/runbooks/signed-deployment-manifest.md).
The runtime paths mirror validator-0:
`/etc/postfiat/releases/combined-devnet-20260921`,
`/opt/postfiat/releases/combined-devnet-20260921/postfiat-node`, and
`/etc/systemd/system/postfiat-validator-N{,-rpc}.service`.

The canonical generator's RPC unit uses `Restart=always`; validator-0's old
unit uses `Restart=on-failure`. Apart from the release ID and this qualified
generator change, the observed validator-0 RPC/transport units are preserved.
No historical drop-ins are added. Topology and circuit metadata are copied
byte-for-byte; all four metadata artifact hashes match this checkout.
Their historical circuit-source revisions describe the circuits, not the new
node build.

The top-level manifest unit/environment pair is the explicit verifier-only
operator reference in `operator/`. It is separate from the twelve validator
bindings, is not a wallet qualification claim, and is not installed on validators.
A scored wallet deployment still needs its own reviewed operator configuration.

The observed `postfiat-cobalt-shadow.service` resolves to the
`cobalt-shadow-e5-8cc7d15e` sidecar. The archive service is
`navcoin-ethereum-archive-rpc-20260907.service`, a transient Python proxy.
Both are outside the supported rollout's target allowlist. Their observed
configuration is retained for health checks; this node release does not redeploy
them. No newer sidecar or archive executable was supplied for this task.

No key material or secret values are committed. Generated environments contain
only public paths, IDs, and readiness/prewarm settings, so no runtime environment
value needed redaction. Key/API credential paths, signing times, and
`@STAGE@` are explicitly marked operator-supplied placeholders. The existing
deployment public file is omitted from Git and pinned by its SHA-256:
`66304dfca0b5893b156eb78e10d65a7b788c262043e85f26eadc82ea86a0314a`.
Never create a replacement publisher key.

Inventory instance IDs/regions come from the retained July fleet rollout packet;
IP addresses match the supplied six hosts. This is an input to fresh Vultr
reconciliation, not evidence that the historical inventory is still current.
